--- src/pages/Reports.tsx
+++ src/pages/Reports.tsx
@@ -1,11 +1,15 @@
+import { useState } from "react";
 import { Card, CardContent, CardHeader, CardTitle } from "@/src/components/ui/card";
 import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, BarChart, Bar, CartesianGrid } from 'recharts';
 import { useStore } from "@/src/lib/store";
 import { useAuthGuard } from "@/src/hooks/useAuthGuard";
 import { Button } from "@/src/components/ui/button";
-import { Download, DollarSign, Users, Briefcase, FileSpreadsheet } from "lucide-react";
+import { Download, DollarSign, Users, Briefcase, FileSpreadsheet, Edit2 } from "lucide-react";
+import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/src/components/ui/dialog";
+import { Label } from "@/src/components/ui/label";
+import { Input } from "@/src/components/ui/input";
 
 export default function Reports() {
-  const { employees, shifts, leaves, timeEntries } = useStore();
+  const { employees, shifts, leaves, timeEntries, updateEmployee } = useStore();
   const { isAdmin } = useAuthGuard({ allowedRoles: ['admin'] });
 
+  const [editOpen, setEditOpen] = useState(false);
+  const [editEmpId, setEditEmpId] = useState<string | null>(null);
+  const [editRate, setEditRate] = useState<number>(25);
+  const [loading, setLoading] = useState(false);
+
   if (!isAdmin) {
     return <div className="p-8 text-center text-muted-foreground">Admin access required to view reports.</div>;
@@ -48,2 +56,15 @@
   };
 
+  const handleEditRateClick = (empId: string, rate: number) => {
+    setEditEmpId(empId);
+    setEditRate(rate);
+    setEditOpen(true);
+  };
+
+  const handleSaveRate = async () => {
+    if (!editEmpId) return;
+    setLoading(true);
+    try {
+      await updateEmployee(editEmpId, { hourly_rate: editRate });
+      setEditOpen(false);
+    } catch (e) {
+      console.error("Failed to update rate", e);
+    } finally {
+      setLoading(false);
+    }
+  };
+
   const exportTimesheets = () => {
@@ -194,4 +215,9 @@
                       <td className="px-4 py-3"><span className="px-2 py-1 bg-slate-100 rounded-md text-xs font-medium">{emp.department}</span></td>
-                      <td className="px-4 py-3 text-right font-mono">${rate.toFixed(2)}/hr</td>
+                      <td className="px-4 py-3 text-right font-mono flex items-center justify-end gap-2">
+                        ${rate.toFixed(2)}/hr
+                        <button onClick={() => handleEditRateClick(emp.id, rate)} className="text-slate-400 hover:text-slate-700 print:hidden">
+                          <Edit2 className="h-3 w-3" />
+                        </button>
+                      </td>
                       <td className="px-4 py-3 text-right font-mono">{hours.toFixed(2)}h</td>
@@ -204,4 +230,22 @@
 
+      <Dialog open={editOpen} onOpenChange={setEditOpen}>
+        <DialogContent>
+          <DialogHeader>
+            <DialogTitle>Adjust Salary Rate</DialogTitle>
+          </DialogHeader>
+          <div className="space-y-4 py-4">
+            <div className="space-y-2">
+              <Label>Hourly Rate ($)</Label>
+              <Input type="number" min="0" step="0.5" value={editRate} onChange={(e) => setEditRate(parseFloat(e.target.value) || 0)} />
+            </div>
+          </div>
+          <DialogFooter>
+            <Button variant="outline" onClick={() => setEditOpen(false)} disabled={loading}>Cancel</Button>
+            <Button onClick={handleSaveRate} disabled={loading}>{loading ? 'Saving...' : 'Save Rate'}</Button>
+          </DialogFooter>
+        </DialogContent>
+      </Dialog>
     </div>
   );
 }
